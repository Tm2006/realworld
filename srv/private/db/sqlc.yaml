version: "2"
sql:
  - engine: sqlite
    schema: "schema.sql"
    queries: "article.sql"
    gen:
      go:
        package: "db"
        out: "gen"
        emit_json_tags: true
        emit_empty_slices: true
        emit_params_struct_pointers: true